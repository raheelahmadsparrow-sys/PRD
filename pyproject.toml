[project]
name = "aw-client-portal"
version = "0.1.0"
description = "AW Client Report Portal — quarterly SACS & TCC report generator for EF Financial Planning"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "jinja2>=3.1",
    "python-multipart>=0.0.12",
    "reportlab>=4.2",
    "pypdf>=6.10.2",
]
